region: eu-west-1
serverless_deployment_bucket: gaia-serverless-deployments-${self:custom.stage}
dev:
  aws_account_id: ${env:AWS_DEV_ACCOUNT_ID}
stg:
  aws_account_id: ${env:AWS_STG_ACCOUNT_ID}
prd:
  aws_account_id: ${env:AWS_PRD_ACCOUNT_ID}